window.onload = startAnimation;                function startAnimation()        {            alert("here");            con = document.getElementById("leftcanvas").getContext("2d");            image = new Image();            image.src = "photos/sjsu4.jpg";            setInterval(leftdraw,50);        }                function leftdraw()        {            const CANVAS_X = 50;            const CANVAS_Y = 50;            const CANVAS_W = 300;            const CANVAS_H = 300;            const IMAGE_W  = 70;            const IMAGE_H  = 70;                         angle = 0;                        con.strokeStyle = "black";            con.fillStyle = "white";            con.fillRect(0, 0, CANVAS_W, CANVAS_H);            con.strokeRect(0, 0, CANVAS_W, CANVAS_H);                        angle += 0.25;            if (angle > 2 *3.14) angle = 0;                        con.save();            con.translate(100, 100);            con.rotate(angle);            con.drawImage(image, -IMAGE_W/2, -IMAGE_H/2,                                   IMAGE_W, IMAGE_H);            con.restore();        }               